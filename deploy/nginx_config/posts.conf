upstream posts_upstream {
    server posts:4000;
}

server {
    listen       80;
    listen       [::]:80;
    server_name  posts.digitalfactory-devops.dz;
    root         /usr/share/nginx/html;
    resolver 127.0.0.11 valid=30s;

    location / {
      if ($host = posts.digitalfactory-devops.dz) {
        return 301 https://$host$request_uri;
      } 
    }
}
server {
    listen              443 ssl;
    server_name         posts.digitalfactory-devops.dz;
    ssl_certificate     /etc/letsencrypt/live/posts.digitalfactory-devops.dz/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/posts.digitalfactory-devops.dz/privkey.pem;
    resolver 127.0.0.11 valid=30s;

    location / {
      proxy_pass http://posts_upstream;

      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection 'upgrade';
      proxy_set_header Host datahub.cureety.dz;
      proxy_cache_bypass $http_upgrade;
    }
}